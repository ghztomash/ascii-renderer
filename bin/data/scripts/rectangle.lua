require("common.variables")

----------------------------------------------------
function setup()
end

----------------------------------------------------
function update()
end

----------------------------------------------------
function draw()
    -- RECTANGLES
    of.drawRectangle(0, 0, dimensions.x, dimensions.y)
end
